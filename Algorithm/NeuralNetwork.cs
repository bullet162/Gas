// using ForecastingGas.Dto.Requests;

// namespace ForecastingGas.Algorithm;

// public class NeuralNetwork
// {

//     /*
//     public string Process(object input) =>
//         input switch
//         {
//             double d        => $"Single value detected: {d}",
//             int i           => $"Integer value detected: {i}",
//             IEnumerable<double> list => $"Collection of doubles, count = {list.Count()}",
//             IEnumerable<int> list    => $"Collection of ints, count = {list.Count()}",
//             null            => "Input is null!",
//             _               => $"Unknown type: {input.GetType().Name}"
//         };
//     */

//     public string CalculateNN(NNInputs inputs)
//     {



//         return "Trained Successfully!";
//     }

// }